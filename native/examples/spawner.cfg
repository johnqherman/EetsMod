# spawner manifest + settings
version = 1.0
author = eetsmod examples
description = spawn objects at the cursor; wheel cycles, F2 labels

# read at runtime via ConfigGet("spawner", "blueprints")
blueprints = marsh_bud_happy, giant_marshmallow, whale, star
